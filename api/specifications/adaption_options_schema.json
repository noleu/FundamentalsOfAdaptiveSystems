{
  "type": "object",
  "properties": {
    "knobs": {
      "type": "object",
      "properties": {
        "routeRandomSigma": {
          "type": "number",
          "description": "the randomization sigma of edge weigths"
        },
        "explorationPercentage": {
          "type": "number",
            "description": "the percentage of routes we want to use for exploration"
        },
        "maxSpeedAndLengthFactor": {
          "type": "integer",
          "description": "how much the length/speed influences the routing",
          "minimum": 0
        },
        "averageEdgeDurationFactor": {
          "type": "integer",
          "description": "how much the averageEdgeFactor influences the routing"
        },
        "freshnessUpdateFactor": {
          "type": "integer",
            "description": "how much the freshnessUpdateFactor influences the routing"
        },
        "freshnessCutOffValue": {
          "type": "integer",
            "description": "if data is older than this we do not consider it in the algorithm"
        },
        "reRouteEveryTicks": {
          "type": "integer",
            "description": "check for a new route very x times after the car starts"
        }
      }
    },
    "totalCarCounter": {
      "type": "integer",
      "description": "the total number of cars in the simulation"
    },
    "edgeAverageInfluence": {
      "type": "integer",
      "description": "how much the average edge duration influences the routing"
    }
  }
}
